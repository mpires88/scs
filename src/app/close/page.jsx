'use client'

import MonthEndClose from '../../components/MonthEndClose'
import { CLIENT_ID } from '../../lib/client'

export default function ClosePage() {
  return <MonthEndClose clientId={CLIENT_ID} />
}
